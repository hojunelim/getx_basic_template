import 'package:flutter/material.dart';

import 'app_colors.dart';

final ThemeData appThemeData = ThemeData(
    primaryColor: spotlightColor,
    colorScheme: ColorScheme.fromSwatch().copyWith(secondary: softBlue));
